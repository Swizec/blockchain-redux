{
    "name": "blockchain-redux",
    "version": "0.2.3",
    "description": "A crypto experiment, Redux store backed by blockchain.",
    "main": "dist/blockchain-redux.js",
    "author": "Swizec Teller",
    "license": "MIT",
    "private": false,
    "dependencies": {
        "crypto-js": "^3.1.9-1"
    },
    "devDependencies": {
        "microbundle": "^0.4.4",
        "reify": "^0.13.3",
        "tap-spec": "^4.1.1",
        "tape": "^4.8.0"
    },
    "scripts": {
        "build": "microbundle -o dist/ --compress false --external all",
        "dev": "microbundle watch",
        "test": "tape -r reify test/*.js | tap-spec"
    },
    "peerDependencies": {
        "firebase": "^4.12.1"
    }
}
